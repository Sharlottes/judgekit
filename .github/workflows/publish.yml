name: Publish to NPM

on: release

jobs:
  build:
    uses: sharlottes/judgekit/.github/workflows/build.yml@master

  publish-npm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    needs: build
    steps:
      - name: Setup Environment
        uses: sharlottes/judgekit/.github/workflows/setup.yml@master

      - name: Download built package
        uses: actions/download-artifact@v3
        with:
          name: built package

      - name: Publish to NPM
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Check NPM Info
        run: npm info judge-toolkit
